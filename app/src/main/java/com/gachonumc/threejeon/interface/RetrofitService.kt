package com.gachonumc.threejeon.`interface`

import okhttp3.Call
import retrofit2.http.Field
import retrofit2.http.POST

interface RetrofitService {
/*
    @POST("/test")
    fun postRequest(
        @Field("store") store: String
    ): Call<ResponseDTO>
    */

}